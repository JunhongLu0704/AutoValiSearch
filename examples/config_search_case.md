# Configuration Search Case

Stage I searches four parameters: `lr`, `lambdap`, `epochp`, and `num_f`. The LLM receives parameter semantics and recent trial feedback, then proposes one next configuration.

The public artifact `artifacts/sample_results/phase1_summary_table.csv` shows the resulting method comparison for PACS and VLCS.
