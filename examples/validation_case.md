# Validation Policy Case

Stage II searches deployable validation policies. A policy combines `source_val` with optional validation views such as color jitter, blur, noise, or crop. The policy must compile under the controller DSL before execution.

The public artifact `artifacts/sample_logs/policy_search_trace.sample.jsonl` includes compact traces for the winning PACS and VLCS validation policies.
